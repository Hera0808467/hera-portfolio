/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/hera-portfolio",
  reactStrictMode: true,
  images: {
    unoptimized: true
  }
};

export default nextConfig;
